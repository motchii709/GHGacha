# GHGacha - GitHub Repository Gacha 仕様

## 概要

指定した言語のGitHubリポジトリをガチャ方式でランダムに引き当て、その情報をリッチなアニメーションとともに表示するWebアプリ。

## 技術スタック

| レイヤー | 技術 | 備考 |
|---------|------|------|
| Frontend | React + Vite + TypeScript | Cloudflare Pagesにデプロイ |
| Backend | Cloudflare Workers (Wrangler) | APIプロキシ + キャッシュ |
| Cache | Cloudflare KV | GitHub APIレスポンスのキャッシュ |
| Client Storage | localStorage | ガチャ履歴・お気に入り |
| デプロイ | Wrangler (Pages + Workers) | `wrangler.toml` で管理 |

## アーキテクチャ

```
User Browser (React SPA)
  ↓ HTTP
Cloudflare Pages (静的アセット)
  ↓
Cloudflare Workers (/api/* にルーティング)
  ├── KV Cache (GitHub API 結果をキャッシュ)
  └── GitHub REST API (未認証: 60回/h → キャッシュで回避)
```

**ポイント**: WorkerがGitHub APIをプロキシし、KVに結果をキャッシュすることで、無料枠のレート制限（60回/h）を実質的に回避。同じ言語の検索は1時間に1回だけGitHub APIを叩く。

## 設計の優先トレードオフ

### 大規模言語の検索問題

JS/Pythonなどは0~100starのリポが数百万あるが、GitHub Search APIは最大1000件までしか取得不可。
→ 1000件からのランダム選択で「その言語のリポジトリをサンプリング」としては十分。

## レアリティ抽選アルゴリズム（戦略Aの課題）

戦略A: `sort=stars&order=desc` で上位100件を取得 → **全員が高starばかりでN/Rが出ない**

**解決策**: 取得した100件のうち順位でレアリティを割り振る（相対レアリティ）

```
100件をstar順にソート → 順位でレアリティ決定:
  Rank  1-  5 ( 5%): UR  ← star数世界一でも最上位5%しかURにならない
  Rank  6- 20 (15%): SR
  Rank 21- 50 (30%): R
  Rank 51-100 (50%): N
```

こうすれば常に4段階のレアリティが均等に出る。star絶対値ではなく「その言語コミュニティ内での相対的な位置づけ」がレアリティになる。

未認証のCloudflare WorkerからのGitHub API呼び出しは**Worker IP単位で60回/h**。

**2つの戦略がある:**

| 戦略 | 内容 | デメリット |
|------|------|-----------|
| **A. 言語単位で全レアリティを一括取得** | `?q=language:typescript&sort=stars&per_page=100` の1リクエストで100件取得し、star数でレアリティ振り分け。1言語=1API | URが引ける確率が極めて低い（100件中ほぼ0） |
| **B. レアリティの抽選 → そのレアリティだけ検索** | 先にレアリティ抽選 → `stars:>10000` など範囲指定。各レアリティ個別にキャッシュ | 言語×レア数=最大120キャッシュ。キャッシュ埋めるのに時間がかかる |

**提案**: A + レアリティ再抽選を組み合わせる
1. 言語選択で100件一括取得しKVにキャッシュ（1h TTL）
2. ガチャ時はキャッシュからレアリティ抽選
3. URが少なすぎる問題 → N/R用の100件とSR/UR用の追加クエリを分ける（計2API/言語）

### 大規模言語の検索問題

JS/Pythonなどは0~100starのリポが数百万あるが、GitHub Search APIは最大1000件しか取得不可。
→ 1000件からランダム選択で「その言語を代表するリポジトリのサンプリング」としては十分。
→ **1000件の壁に引っかからないよう、star範囲を狭めたクエリを工夫する**必要はない（偏らない）。

## 機能一覧（MVP）

### 1. 言語選択画面
- 主要言語をアイコンボタン一覧で表示（20~30言語）
  - TypeScript, JavaScript, Python, Rust, Go, Ruby, Java, Kotlin, Swift, C, C++, C#, PHP, Scala, Dart, Lua, Zig, Elixir, Haskell, OCaml
- 選択中の言語はハイライト表示

### 2. ガチャ演出
- 「ガチャを回す」ボタン → 抽選アニメーション開始
- アニメーション内容:
  - カードがシャッフルされる演出
  - レアリティに応じて光の色が変化
  - カードが反転してリポジトリ情報を表示
- Framer Motion でアニメーション実装

### 3. レアリティシステム

| レアリティ | スター数 | 排出確率 | 表示色 |
|-----------|---------|---------|--------|
| N (Common) | 0~100 | 50% | グレー |
| R (Rare) | 100~1,000 | 30% | ブルー |
| SR (Super Rare) | 1,000~10,000 | 15% | パープル |
| UR (Ultra Rare) | 10,000+ | 5% | ゴールド |

**抽選アルゴリズム**:
1. レアリティを確率テーブルから抽選
2. 該当レアリティのスター数範囲でGitHub Search APIを実行
3. 結果からランダムに1リポジトリを選択

### 4. 結果表示カード

ガチャ結果として表示する情報:
- リポジトリ名 + オーナーアバター
- 説明文
- スター数 / Fork数 / Watch数
- 使用言語（割合）
- トピックタグ
- ライセンス
- READMEプレビュー（最初の30行程度、アコーディオン開閉）
- 最終更新日
- GitHubで開くリンク

### 5. ガチャ履歴（localStorage）
- 過去のガチャ結果を時系列で表示
- 履歴から再度リポジトリを開ける
- 最大50件保存（古いものから削除）

### 6. お気に入り（localStorage）
- 結果カードからスター（お気に入り）登録
- お気に入り一覧画面

## API設計（Cloudflare Workers）

### `GET /api/languages`
- KVにキャッシュされた言語一覧を返す（固定値なので実質スタティック）

### `GET /api/search?language=typescript&rarity=sr`
1. KVキャッシュをチェック（key: `search:typescript:1000..10000`）
2. キャッシュ有 → そのまま返却
3. キャッシュ無 → GitHub Search APIを叩く → KVに保存（TTL: 3600秒）→ 返却
4. 結果からランダムに1件選んで返す

### `GET /api/repo/:owner/:name`
1. KVキャッシュをチェック（key: `repo:owner/name`）
2. リポジトリ詳細（README含む）を返却

## ディレクトリ構成

```
GHGacha/
├── wrangler.toml
├── package.json
├── tsconfig.json
├── vite.config.ts
├── public/
├── src/
│   ├── main.tsx
│   ├── App.tsx
│   ├── components/
│   │   ├── LanguageSelector.tsx    # 言語選択ボタン一覧
│   │   ├── GachaMachine.tsx        # ガチャ抽選・演出コンテナ
│   │   ├── ResultCard.tsx          # 結果表示カード
│   │   ├── RarityBadge.tsx         # レアリティバッジ
│   │   ├── ReadmePreview.tsx       # READMEプレビュー
│   │   ├── PullHistory.tsx         # 履歴一覧
│   │   └── Favorites.tsx           # お気に入り一覧
│   ├── hooks/
│   │   ├── useGacha.ts             # ガチャ抽選ロジック
│   │   ├── useHistory.ts           # localStorage履歴操作
│   │   └── useFavorites.ts         # localStorageお気に入り操作
│   ├── services/
│   │   └── api.ts                  # Worker API呼び出し
│   ├── types/
│   │   └── index.ts                # 型定義
│   └── styles/
│       ├── index.css
│       └── gacha.css               # アニメーション
├── functions/
│   ├── api/
│   │   ├── search.ts               # /api/search ハンドラ
│   │   ├── repo.ts                 # /api/repo/:owner/:name ハンドラ
│   │   └── languages.ts            # /api/languages ハンドラ
│   └── _middleware.ts              # CORSヘッダー等
└── worker-configuration.d.ts
```

## 開発スケジュール（順序）

### Phase 1: プロジェクトセットアップ
- `npm create vite` + React + TypeScript
- Wrangler初期化 (`wrangler.toml`)
- KVネームスペース作成
- Pages + Workersのデプロイ設定

### Phase 2: Worker API実装
- GitHub Search API呼び出し関数
- KVキャッシュレイヤー
- レアリティ抽選ロジック
- `/api/search`, `/api/repo` エンドポイント

### Phase 3: フロントエンド実装
- 言語選択画面
- ガチャ抽選ボタン + 結果表示カード
- READMEプレビュー
- レアリティバッジ表示

### Phase 4: アニメーション演出
- カードシャッフルアニメーション
- レアリティ別エフェクト（光、色）
- カード反転アニメーション

### Phase 5: 履歴・お気に入り
- localStorage永続化
- 履歴一覧画面
- お気に入り登録・一覧

### Phase 6 (post-MVP): GitHub OAuth認証
- GitHub OAuthフロー
- 認証ユーザー向け高レート制限
- クラウド上の履歴保存（D1）

## 注意事項・制約
- GitHub API未認証は60回/h。KVキャッシュで実質的に回避
- Workerの無料枠: 10万リクエスト/日
- KVの無料枠: 1000書き込み/日、10万読み取り/日
- GitHub Search APIは結果が最大1000件まで（それ以上はページネーション不可）
  → 大規模言語（JS, Python等）では1000件をレアリティ範囲で分割する工夫が必要